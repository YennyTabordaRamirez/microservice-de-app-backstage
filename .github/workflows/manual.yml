name: Download ArgoCD Manifests

on:
  push:
    branches:
      - master

jobs:
  download_manifests:
    runs-on: ubuntu-latest
    
    steps:
    # Paso 1: Checkout del repositorio
    - name: Checkout repository
      uses: actions/checkout@v3

    # Paso 2: Instalación de la CLI de OpenShift
    - name: Install OpenShift CLI
      run: |
        curl -L -o oc.tar.gz https://mirror.openshift.com/pub/openshift-v4/clients/ocp/latest/openshift-client-linux.tar.gz
        tar -xvf oc.tar.gz
        sudo mv oc /usr/local/bin/
        sudo chmod +x /usr/local/bin/oc

    # Paso 3: Inicio de sesión en OpenShift
    - name: Login to OpenShift
      run: |
        oc login --token=sha256~j9tqi9iSi78KOTBjH0D8FcxM2qh23JX7WfpA9fS61wg --server=https://c117-e.us-south.containers.cloud.ibm.com:30790

    # Paso 4: Descarga de los Manifestos de ArgoCD
    - name: Download ArgoCD manifests
      run: |
        curl -k -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJhcmdvY2QiLCJzdWIiOiJhZG1pbjphcGlLZXkiLCJuYmYiOjE3MTYyMzI2NjUsImlhdCI6MTcxNjIzMjY2NSwianRpIjoidG9rZW4tYXJnbyJ9.Nb31LPTCecbYgdk3BdpNlhjnmld0nI5VvrGPGXy-ygM" -o bgd2.yaml https://openshift-gitops-server-openshift-gitops.team-kogito-dal10-b3c-4x1-2bd74c909703714447d03dd3414739fe-0000.us-south.containers.appdomain.cloud/api/v1/applications/bdg2

    # Paso 5: Verificación de la existencia del archivo
    - name: Check if file exists
      run: |
        if [ -f "bgd2.yaml" ]; then
          echo "Archivo bgd2.yaml encontrado."
        else
          echo "Archivo bgd2.yaml no encontrado."
          exit 1

    # Paso 6: Visualización de los Manifestos Descargados
    - name: Display downloaded manifests
      run: cat bgd2.yaml
